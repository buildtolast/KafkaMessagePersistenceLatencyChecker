Write a Java interface.
Output path: producer-service/src/main/java/com/codrite/claimcheck/producer/PayloadStore.java
Package: com.codrite.claimcheck.producer
Output EXACTLY this interface (with package declaration), one fenced code block, nothing else:

public interface PayloadStore { String store(String payload, long sizeBytes); }
