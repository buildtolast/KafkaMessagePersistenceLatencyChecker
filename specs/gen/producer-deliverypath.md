Write a Java enum.

Output path: producer-service/src/main/java/com/codrite/claimcheck/producer/DeliveryPath.java
Package: com.codrite.claimcheck.producer

Output EXACTLY ONE top-level type. Exactly ONE fenced code block, nothing outside it.

```java
package com.codrite.claimcheck.producer;

public enum DeliveryPath { INLINE, CLAIM_CHECK }
```

Reproduce exactly this (with the package declaration). No javadoc, no extra members.
